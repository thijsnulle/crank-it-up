import 'package:flutter/material.dart';

final ColorScheme colorScheme = ColorScheme.fromSwatch().copyWith(
  background: const Color(0xFF211D28),
  primary: const Color(0xFF247BA0),
  secondary: Colors.white,
);
